---
import LessonPage from '../../../components/LessonPage.svelte';
import HalfmoonLayout from '../../../layouts/HalfmoonLayout.astro';

const { playlistId, videoId } = Astro.params;
const query = Astro.url.search;
---

<HalfmoonLayout>
  <LessonPage client:only="svelte" {playlistId} {videoId} {query}/>
</HalfmoonLayout>
